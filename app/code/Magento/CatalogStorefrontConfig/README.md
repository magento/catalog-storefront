Magento_CatalogStorefrontConfig module introduces simple configuration reader implementation that allows getting configuration
from deployment configuration files and supports fallback between scopes.
